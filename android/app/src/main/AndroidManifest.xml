<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.vulkan.level" android:required="true"/>
    <uses-feature android:name="android.hardware.vulkan.version" android:required="true"/>

    <application
        android:icon="@mipmap/ic_launcher_pomi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pomi_round"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
        tools:targetApi="31">

        <activity android:name="android.app.NativeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="scene" />
        </activity>

    </application>

</manifest>